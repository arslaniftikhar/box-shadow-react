import {Fragment} from "react";
import Control from "./Control";

const Controls = (props) => {
    const inputChangeHandler = (event) => {

    }
    return (
        <Fragment>
            <Control value={''} onChangeHandler={''}></Control>
        </Fragment>
    )
}

export default Controls;